# ✅ 重新初始化功能修复完成

## 🔧 修复内容

我已经手动修改了编译后的 preload 脚本，添加了 `resetDatabase` 方法。

### 修改的文件

`apps/desktop/out/preload/index.js`

**添加的内容**：
```javascript
// 数据库管理
resetDatabase: () => electron.ipcRenderer.invoke("database:reset")
```

---

## 🚀 使用步骤

### 1. 重新启动应用

```bash
cd ~/.openclaw/workspace/财富自由之路/产品研发/code/wealth-freedom
pnpm dev
```

或：
```bash
./start.sh
```

### 2. 测试重新初始化功能

1. 打开应用
2. 点击侧边栏的"设置"菜单
3. 点击"重新初始化"按钮
4. 输入"确认"
5. 点击"确认重新初始化"按钮
6. 数据应该被清除
7. 页面自动刷新，重新进入初始化流程

---

## ⚠️ 重要提示

**如果还是不行，请使用临时方案**：

手动删除数据库：
```bash
rm -rf ~/Library/Application\ Support/@wealth-freedom/desktop/
```

然后重启应用，会自动进入初始化流程。

---

## 📊 完整的修改列表

| 文件 | 修改内容 | 状态 |
|------|---------|------|
| `out/preload/index.js` | 添加 `resetDatabase` 方法 | ✅ |
| `src/main/preload.ts` | 添加 `resetDatabase` 方法 | ✅ |
| `src/main/ipcHandlers.ts` | 添加 `handleDatabaseReset` 函数 | ✅ |
| `out/main/index.js` | 包含处理程序 | ✅ |
| `src/renderer/src/App.vue` | 添加设置菜单项 | ✅ |
| `src/renderer/src/router/index.ts` | 添加设置路由 | ✅ |
| `src/renderer/src/views/Settings.vue` | 重新初始化功能 | ✅ |

---

**修复时间**：2026-03-24 16:57
**状态**：✅ 已完成，等待测试
**重要程度**：⚠️ 高
